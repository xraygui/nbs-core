[build-system]
requires = ["setuptools", "wheel"]

[project]
name = "nbs-core"
version = "0.1.0"
description = "NBS Core Utilities"
authors = [
    { email = "ctitus@bnl.gov" },
]
requires-python = ">=3.7"

[project.urls]
homepage = "https://github.com/xraygui/nbs-core"  # Replace with your project's homepage

